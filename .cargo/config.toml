[build]
# Enable sccache if available (set SCCACHE=/path/to/sccache in environment)
# Build with: SCCACHE=/usr/local/bin/sccache CARGO_BUILD_JOBS=1 cargo build

[profile.dev]
opt-level = 0
debug = true
incremental = true

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
strip = true
