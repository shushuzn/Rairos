[package]
name = "traitclaw_integration"
version = "0.1.0"
edition = "2021"
publish = false

[workspace]

[dependencies]
# For Rairos types (mock for POC)
serde = { version = "1", features = ["derive"] }
serde_json = "1"
uuid = { version = "1", features = ["v4", "serde"] }
