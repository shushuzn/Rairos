[package]
name = "rairos-gene-pool-watcher"
version = "0.1.0"
edition = "2021"

[dependencies]
rairos-core = { path = "../rairos-core" }
serde = { workspace = true, features = ["derive"] }
serde_json.workspace = true
chrono = { workspace = true, features = ["serde"] }
dirs.workspace = true
rairos-gene-pool-io = { path = "../rairos-gene-pool-io" }

[dev-dependencies]
tempfile.workspace = true
