[package]
name = "rairos-argument-builder"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { workspace = true, features = ["derive"] }
thiserror = "2.0"

[dev-dependencies]
serde_json.workspace = true
tempfile.workspace = true
