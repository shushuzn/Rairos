[package]
name = "rairos-paradigm"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json.workspace = true

[lints.rust]
unsafe-code = "forbid"