[package]
name = "rairos-progress-tracker"
version = "0.1.0"
edition = "2021"

[dependencies]
chrono = "0.4"
once_cell = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
