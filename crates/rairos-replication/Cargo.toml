[package]
name = "rairos-replication"
version = "0.1.0"
edition = "2021"

[dependencies]
regex.workspace = true
serde = { version = "1.0", features = ["derive"] }
serde_json.workspace = true

[lints.rust]
unsafe-code = "forbid"