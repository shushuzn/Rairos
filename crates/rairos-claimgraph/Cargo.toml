[package]
name = "rairos-claimgraph"
version = "0.1.0"
edition = "2021"

[lib]
name = "rairos_claimgraph"
crate-type = ["rlib"]

[dependencies]
serde = { workspace = true, features = ["derive"] }
serde_json.workspace = true
regex.workspace = true
dirs.workspace = true
chrono = { workspace = true, features = ["serde"] }
