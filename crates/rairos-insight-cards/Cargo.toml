[package]
name = "rairos-insight-cards"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { workspace = true, features = ["derive"] }
serde_json.workspace = true
chrono = { version = "0.4", features = ["serde"] }
dirs.workspace = true
regex.workspace = true

[dev-dependencies]
tempfile.workspace = true
