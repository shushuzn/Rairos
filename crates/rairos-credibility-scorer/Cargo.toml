[package]
name = "rairos-credibility-scorer"
version = "0.1.0"
edition = "2021"

[dependencies]
rairos-gene-pool-io = { path = "../rairos-gene-pool-io" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[dev-dependencies]
tempfile = "3.10"
