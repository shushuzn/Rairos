[package]
name = "rairos-sections"
version = "0.1.0"
edition = "2021"

[dependencies]
rairos-pdf = { path = "../rairos-pdf" }
serde = { workspace = true, features = ["derive"] }
serde_json.workspace = true
thiserror = "2.0"
regex.workspace = true
chrono = { workspace = true, features = ["serde"] }

[features]
default = []
