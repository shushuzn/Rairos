[package]
name = "rairos-research-memory"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { workspace = true, features = ["derive"] }
serde_json.workspace = true
chrono = { workspace = true, features = ["serde"] }
dirs.workspace = true
uuid = { workspace = true, features = ["v4", "serde"] }

[dev-dependencies]
