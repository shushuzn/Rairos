[package]
name = "rairos-gap-detector"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json.workspace = true
chrono.workspace = true
dirs.workspace = true
thiserror = "2.0"

[dev-dependencies]
tempfile.workspace = true

